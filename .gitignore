# 비밀번호가 평문으로 들어있는 SQL 파일들 — Supabase 대시보드에 직접 붙여넣어 실행하고
# GitHub에는 절대 올리지 않는다. 백업은 1Password/iCloud Keychain 등 비밀번호 관리도구에.
SUPABASE_바로붙여넣기.sql
SUPABASE_토큰_마이그레이션.sql
SUPABASE_설정.sql

# macOS
.DS_Store

# 아카이브
*.zip

# 에디터 임시
.vscode/
.idea/
*.swp
*~

# Claude Code / oh-my-claudecode 세션 (대화 로그 — 공개 금지)
.omc/
.claude/

# 노드(향후 회귀 테스트 추가 시)
node_modules/

# 배포 산출물 — 전에는 저장소 밖에 있던 폴더다
_배포/

# 보관용 v2.1 구현 — 관리자 비밀번호로 Supabase RPC를 호출하는 코드가 들어 있다.
# 로컬에만 두고 공개 사이트에 올리지 않는다.
_archive_v2/index_v2.1_DEPRECATED.html
_archive_v2/handoff_v2.1/
_보안_조치.md
