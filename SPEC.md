# 고교선택검사 v3.1 — 마스터 스펙

> 2026-08-21 전면 개정. v2.1 문서는 `_archive_v2/`로 옮겼고 더 이상 기준이 아니다.

## 0. 단일 진실 공급원

```
SPEC.md          사람이 읽는 최상위 설계        ← 이 문서
QUESTIONS.json   화면에 노출되는 문항
RULES.json       판정 로직
index.html       위 셋의 구현
```

**네 파일이 일치해야 한다.** 어긋나면 SPEC → QUESTIONS → RULES → index 순으로 맞춘다.
**"충돌 시 index.html이 정답"은 폐기했다.** `WEIGHTS.json`도 폐기했다.

## 1. 이 검사가 하는 일과 안 하는 일

학부모가 결과를 읽고 세 가지를 알면 성공이다.

1. 우리 아이는 어떤 조건에서 공부가 잘 유지되는가
2. 어떤 학교 환경에서는 문제가 생길 가능성이 있는가
3. 학교를 실제로 비교할 때 무엇을 확인해야 하는가

**안 하는 것.** 아이별 예상 내신 등급, 상위 몇 퍼센트, 적합도 점수, 심리 유형 이름 붙이기,
흥미 여섯 문항으로 직업·학과 찍기, 초등생에게 고교 유형 순위 주기.

결과 하단 고정 문구는 이것이다.

> 이 검사는 고교 진학 후 내신 등급을 예측하지 않습니다. 현재 성적과 공부 성향, 통학·비용 등의 조건을 바탕으로 어떤 학교 환경을 먼저 살펴볼지 정리하는 검사입니다.

## 2. 문항 구조

| 블록 | 무엇을 재나 | 문항 | 응답자 |
|---|---|---|---|
| A | 학년과 실제로 선택 가능한 범위 | 6 | 보호자 |
| B | 현재 학력 위치 | 3~4 (중1~중3만) | 보호자 |
| C | 범위가 있는 시험과 없는 시험에서의 차이 | 2 | 보호자 |
| D | 자기 기준에 미달했을 때의 반응 | 3 | 보호자 |
| E | 주변의 잘하는 아이를 공부 동력으로 바꾸는 정도 | 2 | 보호자 |
| F | 결과가 나쁜 뒤 공부 행동으로 돌아오는 속도 | 2 | 보호자 |
| G | 공부 순서와 계획을 스스로 잡는 정도 | 3 | 보호자 |
| H | 과학고·영재학교 준비 여부 | 1 (중2·중3, 조건부) | 보호자 |
| I | 자유서술 | 2 (선택입력) | 보호자 |
| K | 또래 환경·자기관리·관심 분야·흥미 | 10 | 아이 |
| TB | 후보가 둘 이상 남았을 때 우선순위 | 2 (조건부) | 보호자 |

**중2·중3 기준 핵심 31문항**(성적표 숫자 경로), 선택입력 2, 조건부 3.
**초등은 27문항.** 학력 블록과 관심 분야 직접 선택을 건너뛴다.

## 3. 척도

| 이름 | 칸 | 라벨 |
|---|---|---|
| `freq5` | 5 | 거의 매번 · 자주 · 가끔 · 드물게 · 전혀 없다 |
| `interest4` | 4 | 별로 해보고 싶지 않다 · 조금 · 꽤 · 꼭 해보고 싶다 |
| `binary` | 2 | 양쪽 모두 정상적인 성향으로 읽히게 쓴다 |
| `ordinal4` | 4 | 기간·정도 |

**빈도와 강도를 섞지 않는다.** 전에는 "매우 그렇다"(강도)와 "가끔 그렇다"(빈도)가 한 척도에 있었다.
**"보통이다"·"반반이다"를 두지 않는다.** `freq5`의 가운데 "가끔"은 도피 선택지가 아니라 실제 답이다.

## 4. 판정 — 점수를 매기지 않고 순차로 거른다

```
1단계  실제로 선택 가능한 범위      통학·기숙은 하드, 비용은 소프트
2단계  현재 학력 위치              중1~중3만. 화면에 숫자를 안 낸다
3단계  그 환경에서 공부가 유지될 가능성  ★ 유형을 탈락시키지 않는다
4단계  교육과정과 관심 분야
5단계  세 묶음으로 분류
```

### 4-1. 하나의 축이 모든 학교를 서열화하지 않는다

v2가 실패한 이유가 이것이다. "좋은 아이 점수"가 높을수록 일반고 → 자사고 → 특목고로 올라가는 구조였다.

**각 변수는 "어떤 환경에서 공부가 유지되는가"에만 쓴다.**
자기관리가 높다는 사실이 "자사고 적합"을 뜻하지 않는다. 경쟁을 동력으로 쓴다는 사실도 마찬가지다.

### 4-2. 성향 하나로 유형을 탈락시키지 않는다

3단계는 유형을 **떨어뜨리지 않는다.** "조건 확인" 묶음으로 옮기고 이유 문장을 붙인다.
회복이 느리다는 답 하나로 자사고를 없애면, 그 판단의 근거가 검사에 없다.

실제로 후보에서 빼는 것은 **통학·기숙(물리적으로 닿지 않음)**과 **학력 위치가 지원자 범위 밖**뿐이다.
비용은 같은 유형 안에서도 학교마다 달라 하드 탈락시키지 않는다.

### 4-3. 세 묶음

| 묶음 | 조건 |
|---|---|
| 현재 우선 검토할 수 있는 유형 | 현실 조건 통과 + 학력 안쪽 + 환경 우려 없음 |
| 조건을 확인한 뒤 검토할 유형 | 환경 우려·비용 조정·학력 바깥·교육과정 확인 필요 |
| 현재 우선순위가 낮은 유형 | 통학·기숙으로 닿지 않거나 지원자 범위 밖 |

묶음 안에서 점수로 줄 세우지 않는다.

## 5. 학년별로 결과가 다르다

| 학년 | 유형 순위 | 결과 |
|---|---|---|
| 초3·4 | **없음** | 공부 환경 성향 · 관리 필요 정도 · 경쟁 반응 · 관심 활동 · 앞으로 관찰할 것 |
| 초5·6 | **없음** | 위 + 중학교에서 확인할 것 2~3개 |
| 중1 | 있음 | "1학년 성적은 변동 폭이 큽니다. 1년 뒤에 다시 확인해 보세요" 명시 |
| 중2·중3 | 있음 | 전체 |
| 고1 이상 | **없음** | 현재 학교에서 확인할 것. 전학·재입학 관점 |

**초등에 유형 순위를 주지 않는 이유.** 3년 뒤 성적을 모르는데 유형을 찍으면 그것이 예언이 된다.

## 6. 결과 문장 원칙

**응답 → 해석 → 학교 선택에 미치는 영향** 순서로 쓴다.

나쁨: `아이는 경쟁에서 빛나는 자극형입니다.`
좋음: `잘하는 친구가 있을 때 공부량이 늘어난다고 답하셨습니다. 경쟁이 부담으로만 작용하는 아이는 아닙니다. 학업 수준이 높은 학교를 볼 때 이 점은 장점이 될 수 있습니다.`

나쁨: `회복력이 낮아 자사고는 위험합니다.`
좋음: `시험 결과가 좋지 않을 때 공부 리듬을 되찾는 데 시간이 걸린다고 답하셨습니다. 경쟁이 강한 학교를 볼 때는 성적보다 먼저 이 반응을 확인할 필요가 있습니다.`

### 금지 표현

심리 유형 이름 전부(연료형·자극형·자책형·취약형·소진형·관리형·강철 멘탈·탐험형·균형형),
`빛납니다` `붙잡으세요` `환경 자체가 위험` `안전한 선택지` `가장 흔한 자리` `종합 적합도`.

특히 **취약·소진·자책은 아이에게 낙인으로 읽힌다.** 내부 코드명으로도 쓰지 않는다.

### 전문용어

`세특` `학종` `비교과` `교과·지역균형 전형`은 초중등 학부모 화면에서 쓰지 않는다.
다만 `수능` `내신` `자사고` `외고`처럼 학부모가 이미 아는 말까지 억지로 풀어 쓰지 않는다. 자연스러움이 우선이다.

## 7. 흥미 문항

여섯 계열을 **각각 독립적으로** 4단계로 묻는다. 양자택일로 하나를 버리게 하지 않는다.

전에는 `아픈 사람 옆에서 돕기` 대 `코드를 짜서 움직이게 하기` 식이었다.
앞은 착해 보이는 답이고 뒤는 경험이 있어야 고른다. 관심이 아니라 다른 것을 쟀다.

**판정.** 상위 두 계열까지만 "상대적으로 더 관심을 보인 활동"으로 표시한다.
한 계열을 적성으로 확정하지 않고, 직업이나 학과를 지정하지 않는다.

**아이가 관심 분야를 직접 골랐으면 그쪽을 우선한다.** 흥미 문항은 대조 정보다.
어긋나면 "틀렸다"가 아니라 "두 방향의 활동을 모두 경험해 보는 편이 좋겠습니다"로 쓴다.

## 8. 응답 성의 확인

**모순 쌍을 쓰지 않는다.** 전에 쓰던 세 쌍은 서로 다른 상황이라 같은 답을 기대할 근거가 없었다.

- 응답 속도가 문항당 평균 3초 미만
- `freq5` 네 문항과 `interest4` 여섯 문항이 **둘 다** 한 값으로 직진

**한쪽만으로는 불성실로 보지 않는다.** A를 여러 번 골랐다는 이유만으로 표시하지 않는다.
문구는 의심하는 말투를 쓰지 않는다. "답변이 빠르게 진행된 문항이 몇 개 있습니다."

## 9. 물어놓고 안 쓰는 문항 0개

모든 채점·진단 문항에 `decisionUse`를 단다. RULES나 결과 설명 어느 쪽에서도 참조되지 않으면 빌드 테스트가 실패한다.

## 10. 저장

기존 Supabase 테이블(`public.responses`, `payload jsonb`)을 그대로 쓴다.
새 응답에는 `schemaVersion: "v3.1"`을 넣는다. 과거 v2 응답은 관리자 화면에서 버전을 표시하고 구버전 형식으로 열람만 한다.

## 11. 확인이 필요한 항목 (코드와 분리)

- [ ] 과학고·영재학교 전형 일정 — 학년별 안내 문구를 쓰기 전에 해당 연도 요강 원문 확인
- [ ] 지역인재 요건 — 결과에 문장으로 쓰려면 시행령 원문 재확인
- [ ] 집단 퍼센트 4개 — 해당 학년도 모집 정원으로 재계산

**임의로 최신 법·일정을 만들어 내지 않는다.**
