# 고교선택검사 v3.1 — 마스터 스펙

> 2026-08-21 전면 개정. v2.1 문서는 `_archive_v2/`로 옮겼고 더 이상 기준이 아니다.

## 0. 단일 진실 공급원

```
SPEC.md          사람이 읽는 최상위 설계        ← 이 문서
QUESTIONS.json   화면에 노출되는 문항
RULES.json       판정 로직
index.html       위 셋의 구현
```

**네 파일이 일치해야 한다.** 어긋나면 SPEC → QUESTIONS → RULES → index 순으로 맞춘다.
**"충돌 시 index.html이 정답"은 폐기했다.** `WEIGHTS.json`도 폐기했다.

## 1. 이 검사가 하는 일과 안 하는 일

학부모가 결과를 읽고 세 가지를 알면 성공이다.

1. 우리 아이는 어떤 조건에서 공부가 잘 유지되는가
2. 어떤 학교 환경에서는 문제가 생길 가능성이 있는가
3. 학교를 실제로 비교할 때 무엇을 확인해야 하는가

**안 하는 것.** 아이별 예상 내신 등급, 상위 몇 퍼센트, 적합도 점수, 심리 유형 이름 붙이기,
흥미 여섯 문항으로 직업·학과 찍기, 초등생에게 고교 유형 순위 주기.

결과 하단 고정 문구는 이것이다.

> 이 검사는 고교 진학 후 내신 등급을 예측하지 않습니다. 현재 성적과 공부 성향, 통학·비용 등의 조건을 바탕으로 어떤 학교 환경을 먼저 살펴볼지 정리하는 검사입니다.

## 2. 문항 구조

| 블록 | 무엇을 재나 | 문항 | 응답자 |
|---|---|---|---|
| A | 학년과 실제로 선택 가능한 범위 (A6 이사 여부가 학군지 일반고를 가른다) | 6 | 보호자 |
| B | 현재 학력 위치 | 3~4 (중1~중3만) | 보호자 |
| C | 범위가 있는 시험과 없는 시험에서의 차이 | 2 | 보호자 |
| D | 자기 기준에 미달했을 때의 반응 | 3 | 보호자 |
| E | 주변의 잘하는 아이를 공부 동력으로 바꾸는 정도 | 2 | 보호자 |
| F | 결과가 나쁜 뒤 공부 행동으로 돌아오는 속도 | 2 | 보호자 |
| G | 공부 순서와 계획을 스스로 잡는 정도 | 3 | 보호자 |
| H | 과학고·영재학교 준비 여부 | 1 (중2·중3, 조건부) | 보호자 |
| I | 자유서술 | 2 (선택입력) | 보호자 |
| K | 또래 환경·자기관리·관심 분야·흥미 | 10 | 아이 |
| TB | 후보가 둘 이상 남았을 때 우선순위 | 2 (조건부) | 보호자 |

**중2·중3 기준 핵심 31문항**(성적표 숫자 경로), 선택입력 2, 조건부 3.
**초등은 27문항.** 학력 블록과 관심 분야 직접 선택을 건너뛴다.

## 3. 척도

| 이름 | 칸 | 라벨 |
|---|---|---|
| `freq5` | 5 | 거의 매번 · 자주 · 가끔 · 드물게 · 전혀 없다 |
| `interest4` | 4 | 별로 해보고 싶지 않다 · 조금 · 꽤 · 꼭 해보고 싶다 |
| `binary` | 2 | 양쪽 모두 정상적인 성향으로 읽히게 쓴다 |
| `ordinal4` | 4 | 기간·정도 |

**빈도와 강도를 섞지 않는다.** 전에는 "매우 그렇다"(강도)와 "가끔 그렇다"(빈도)가 한 척도에 있었다.
**"보통이다"·"반반이다"를 두지 않는다.** `freq5`의 가운데 "가끔"은 도피 선택지가 아니라 실제 답이다.

## 4. 판정 — 점수를 매기지 않고 순차로 거른다

```
1단계  실제로 선택 가능한 범위      통학·기숙은 하드, 비용은 소프트
2단계  현재 학력 위치              중1~중3만. 화면에 숫자를 안 낸다
3단계  그 환경에서 공부가 유지될 가능성  ★ 유형을 탈락시키지 않는다
4단계  교육과정과 관심 분야
5단계  세 묶음으로 분류
```

### 4-1. 하나의 축이 모든 학교를 서열화하지 않는다

v2가 실패한 이유가 이것이다. "좋은 아이 점수"가 높을수록 일반고 → 자사고 → 특목고로 올라가는 구조였다.

**각 변수는 "어떤 환경에서 공부가 유지되는가"에만 쓴다.**
자기관리가 높다는 사실이 "자사고 적합"을 뜻하지 않는다. 경쟁을 동력으로 쓴다는 사실도 마찬가지다.

### 4-2. 성향 하나로 유형을 탈락시키지 않는다

3단계는 유형을 **떨어뜨리지 않는다.** "조건 확인" 묶음으로 옮기고 이유 문장을 붙인다.
회복이 느리다는 답 하나로 자사고를 없애면, 그 판단의 근거가 검사에 없다.

실제로 후보에서 빼는 것은 **통학·기숙(물리적으로 닿지 않음)**과 **학력 위치가 지원자 범위 밖**뿐이다.
비용은 같은 유형 안에서도 학교마다 달라 하드 탈락시키지 않는다.

### 4-2-1. 학교 유형은 다 이름을 낸다

칼럼과 7월 세미나가 다루는 유형에 학군지 일반고를 더해 여덟 개를 쓴다. **일반고(자율형 공립고 포함) · 학군지 일반고 · 광역 자사고 · 전국 자사고 · 외고 · 국제고 · 과학고 · 영재학교.**
집단 퍼센트는 세미나 값이다. 영재 1 · 과학고 2 · 전국자사고 5 · 외고·국제고 10 · 광역자사고 25 · 일반고 100.

**관심·준비가 맞는 유형만 후보로 낸다.** 칼럼도 그 아이에게 맞는 유형만 짚는다.
- 외고 — 아이가 외국어에 관심을 보였을 때만
- 국제고 — 국제에 관심을 보였을 때만
- 과학고·영재학교 — 이공·메디컬 관심 + H1이 준비 중/관심일 때만
- 일반고·학군지 일반고·광역자사고·전국자사고 — 항상 후보

맞지 않아 뺀 유형은 **왜 뺐는지**를 결과에 적는다. "외국어 관심이 뚜렷하지 않아 외고는 뺐습니다."

**과학고·영재학교는 내신 등급 컷을 적용하지 않는다.** 내신이 아니라 별도 전형(지필·자기주도학습)으로 뽑기 때문이다. 세미나도 "등급 계산 대상이 아니다"로 못 박았다. 후보로 이름은 내되 "전형 준비가 필요하다"를 반드시 붙인다.

### 4-2-1-1. 학군지 일반고는 성적이 아니라 사는 곳으로 갈린다

학부모가 실제로 고민하는 갈림길이 "일반고냐 갓반고냐"인데 v3.1까지는 일반고 하나로 뭉쳐 있었다. 그래서 여덟 번째 유형으로 세운다.

**이 유형은 성적으로 뽑는 학교가 아니라 배정받는 학교다.** 그래서 `assigned: true`를 달고, 학력 위치로 후보에서 떨어뜨리지 않는다. 하위권이어도 이사만 가면 그 학교에 배정된다. 대신 `dense: true`라 기질 신호와 학력 위치가 뒤면 조건 확인으로 옮긴다. 붙는 문장은 "못 간다"가 아니라 **"들어가도 내신 등수는 뒤로 밀린다"**다.

| 사는 곳 | 이사(A6) | 묶음 |
|---|---|---|
| 학군지 안 | 무관 | 유지 (거주 판정 문장) |
| 학군지 밖 | 고려 가능 | 조건 확인 (이사 전제) |
| 학군지 밖 | 거주지 유지 | 우선순위 낮음 |

**대가를 반드시 같이 적는다.** 수업 진도·또래·입시 정보는 앞서고 내신 등수는 밀린다. 한쪽만 적으면 이 유형이 추천이 된다.

**목록은 우리가 정한 것이고, 그렇다고 화면에 적는다.** 전국 시·군·구 단위 학군지에는 공식 근거가 없다. 근거가 되는 건 학원·교습소 수 공시(서울시교육청 2023 — 대치동 1609 · 목동 1052 · 신정동 887 · 중계동 601 · 상계동 494) 하나뿐이고 서울에만 있다. 나머지는 시·도교육청 평준화 학군 단위와 입시업계 통념이다. `RULES.json`의 `hakgunji.basis`·`unverified`·`caption`에 이걸 적어 두고, 결과 화면에도 "공식 구분이 아니다 · 여기 없는 지역이라도 학교별로 확인하면 된다"를 낸다.

**단위는 시·군·구다.** A2가 시·도와 시·군·구까지만 받는다. 동 단위로 내려가면 검사가 받는 답과 안 맞고 틀리기 쉽다.

**비수도권 의약 지망에게는 지역인재를 먼저 보여 준다.** 이 유형이 이사를 전제하는 이상 피할 수 없는 경고다. 비수도권에 살면서 메디컬에 관심이 있고 이 유형이 후보에 남아 있으면 `hakgunji.regionalTalent` 문장을 붙인다. **권역 일치가 아니라 비수도권이면 자격이 유지된다.** 자격이 진짜 사라지는 건 수도권(서울·경기·인천) 소재 학교뿐이다. 이 구분을 뭉개면 CLAUDE.md의 2026-07-13 실패를 그대로 되풀이한다.

### 4-2-1-2. 평준화면 학교를 고르는 게 아니라 지망 순서를 정하는 것이다

배정 유형(일반고·학군지 일반고)에서 학부모가 할 일이 사는 곳에 따라 정반대다.

- **평준화 지역** — 성적으로 학교가 정해지지 않는다. 지망한 학교 중에서 추첨으로 배정한다. 그래서 할 일은 "어느 학교에 갈 수 있나"가 아니라 **"1지망을 어디로 쓰나"**다.
- **비평준화 지역** — 학교마다 성적으로 뽑는다. 지원할 학교의 지난해 합격선을 본다.

이 구분이 없으면 배정 유형 칸이 헛말을 한다. 평준화 지역 학부모에게 "성적 위치를 확인하세요"만 적으면 정작 그 집이 실제로 손을 댈 수 있는 지망 순서 이야기가 빠진다.

**한 시·군 안에서 갈리면 어느 쪽으로도 단정하지 않는다.** 창원시는 진해구만 비평준화이고, 청주·충주·군산·익산·제주시·포항시는 동지역만 평준화다. 학부모가 "창원시"라고만 적으면 진해구인지 알 수 없으니 **갈린다는 사실과 양쪽을 그대로 적고 확인하라고 한다.** 구까지 적으면 그때 확정한다. 판정은 세 단계로 좁힌다 — 이름이 정확히 같은 것 → 앞부분이 같은 것 → 시·군 이름이 같은 것. 먼저 걸리는 단계가 이긴다.

**확인된 시·도만 말한다.** `confidence`가 `확인`인 시·도만 화면에 낸다. 혼재 시·도인데 시·군·구를 안 적었으면, 또 어느 목록에도 없는 시·군·구면 아무 말도 하지 않는다. **모르는 배정 방식을 그럴듯하게 지어내지 않는다.**

값마다 `schoolYear`·`sourceUrl`·`directRead`를 같이 둔다. 원문을 못 열었으면 `directRead: false`이고 그때는 `confidence`가 `확인`일 수 없다. `tests/run.mjs`의 I35·I36·I37이 이 규칙을 지킨다.

**긴 단서는 `근거_평준화_2027.md`에 둔다.** 시·도별 원문 제목·지망 상세·단서·반박 79건이 거기 있다. `RULES.json`에는 화면과 판정에 필요한 것만 남긴다. 단서까지 넣었더니 화면에 안 나가면서 `index.html`만 115KB에서 168KB로 불었다.

**이번 학기에 할 일도 같이 바뀐다.** 평준화면 `nextAction`이 "배정받고 싶은 학교 세 곳을 순서까지 정해 적어 보세요"로 간다.

### 4-2-2. 유형마다 학교 이름을 적는다

유형 이름만 보고는 학부모가 어디를 말하는지 모른다. 그래서 묶음 안의 유형마다 **실제로 그 유형에 들어가는 학교 이름**을 함께 낸다. `RULES.json`의 `schoolExamples`가 그 목록이다.

**추천이 아니라 목록이다.** 어느 학교를 가라고 찍지 않는다. 순서도 지역·설립 순이지 우열이 아니다.

**지역 단위로 뽑는 유형은 사는 시·도의 학교만 낸다.** 광역 자사고·외고·국제고·과학고가 여기 해당한다. 부산 사는 집에 서울 외고 여섯 곳을 보여 주면 못 가는 학교를 후보로 읽는다. 그 시·도에 없으면 이름 대신 "○○에는 없습니다 + 다른 지역 지원 가능 여부"를 낸다.
**전국에서 뽑는 유형은 전체를 낸다.** 전국 자사고 10곳, 영재학교 8곳이다. 하나고는 지정 유형이 전국단위지만 일반전형이 서울 거주자만 받아 그 단서를 붙인다.
**일반고와 학군지 일반고는 학교 이름을 안 낸다.** 둘 다 배정받는 학교라서다. 학군지 일반고는 대신 학군지로 잡은 시·군·구 목록을 보여 준다 — 학군지 밖에 살면서 이사를 고려하는 가정에게는 그게 다음 질문이다. 우선순위 낮음으로 간 경우에는 목록도 안 낸다.
**일반고는 이름을 안 낸다.** 배정이 거주지로 갈려 미리 못 적는다. 대신 학교알리미에서 그 시·군·구 학교를 찾아 후보 세 곳을 비교하는 절차를 낸다.

**학교 수는 98개다.** 광역 자사고 22(서울 14 + 서울 외 8) · 전국 자사고 10 · 외고 28 · 국제고 8 · 과학고 22 · 영재학교 8. `tests/run.mjs`의 I29가 이 합계를 지킨다.

**출처는 하나로 묶는다.** 2027학년도 자기주도학습전형 및 고등학교 입학전형 영향평가 매뉴얼(한국교육개발원 CRM 2026-19) 원문, 학교별 2027 모집요강, 그리고 `자소서첨삭기/근거대장_2027.md`. 세 경로가 같은 값이라 그대로 쓴다. 학교가 새로 생기거나 지정이 풀리면 첨삭기 `SCHOOLS`를 먼저 고치고 여기로 옮긴다.

**점수만으로는 최상위를 못 가른다.** 중학교 올 A는 상위 28퍼센트라 최상위가 뭉친다(세미나 §반론). 그래서 간이 경로(점수대)로는 자사고·특목고 최상위 판정이 어렵고, 3월 모의 최상위가 함께일 때만 상위 3퍼센트로 본다. 자사고·특목을 함께 보려면 성적표 숫자 경로로 안내한다.

### 4-3. 세 묶음

| 묶음 | 조건 |
|---|---|
| 현재 우선 검토할 수 있는 유형 | 현실 조건 통과 + 학력 안쪽 + 환경 우려 없음 |
| 조건을 확인한 뒤 검토할 유형 | 환경 우려·비용 조정·학력 바깥·교육과정 확인 필요 |
| 현재 우선순위가 낮은 유형 | 통학·기숙으로 닿지 않거나 지원자 범위 밖 |

묶음 안에서 점수로 줄 세우지 않는다.

## 5. 학년별로 결과가 다르다

| 학년 | 유형 순위 | 결과 |
|---|---|---|
| 초3·4 | 순위 없음 · **방향 있음** | 환경의 결(학업 분위기 / 관리 / 자신감) · 관심이 이어지면 어떤 길이 있는지 알아두기 |
| 초5·6 | 순위 없음 · **방향 있음** | 위 + 중학교에서 확인할 것(첫 성적표 · 고1 3모 한 과목) |
| 중1 | 있음 | "1학년 성적은 변동 폭이 큽니다. 1년 뒤에 다시 확인해 보세요" 명시 |
| 중2·중3 | 있음 | 전체 |
| 고1 이상 | **없음** | 현재 학교에서 확인할 것. 전학·재입학 관점 |

**초등에 유형 순위를 주지 않는 이유.** 3년 뒤 성적을 모르는데 유형을 찍으면 그것이 예언이 된다.
대신 **방향**은 낸다. 지금 성향에서 보이는 환경의 결과, 관심이 이어지면 어떤 선택지가 있는지 알아두기까지다. 유형 이름을 "맞다"로 단정하지 않고, "몇 년 사이 달라진다, 지금 정할 일이 아니다"를 반드시 붙인다.

## 6. 결과 문장 원칙

**응답 → 해석 → 학교 선택에 미치는 영향** 순서로 쓴다.

나쁨: `아이는 경쟁에서 빛나는 자극형입니다.`
좋음: `잘하는 친구가 있을 때 공부량이 늘어난다고 답하셨습니다. 경쟁이 부담으로만 작용하는 아이는 아닙니다. 학업 수준이 높은 학교를 볼 때 이 점은 장점이 될 수 있습니다.`

나쁨: `회복력이 낮아 자사고는 위험합니다.`
좋음: `시험 결과가 좋지 않을 때 공부 리듬을 되찾는 데 시간이 걸린다고 답하셨습니다. 경쟁이 강한 학교를 볼 때는 성적보다 먼저 이 반응을 확인할 필요가 있습니다.`

### 금지 표현

심리 유형 이름 전부(연료형·자극형·자책형·취약형·소진형·관리형·강철 멘탈·탐험형·균형형),
`빛납니다` `붙잡으세요` `환경 자체가 위험` `안전한 선택지` `가장 흔한 자리` `종합 적합도`.

특히 **취약·소진·자책은 아이에게 낙인으로 읽힌다.** 내부 코드명으로도 쓰지 않는다.

### 전문용어

`세특` `학종` `비교과` `교과·지역균형 전형`은 초중등 학부모 화면에서 쓰지 않는다.
다만 `수능` `내신` `자사고` `외고`처럼 학부모가 이미 아는 말까지 억지로 풀어 쓰지 않는다. 자연스러움이 우선이다.

## 7. 흥미 문항

여섯 계열을 **각각 독립적으로** 4단계로 묻는다. 양자택일로 하나를 버리게 하지 않는다.

전에는 `아픈 사람 옆에서 돕기` 대 `코드를 짜서 움직이게 하기` 식이었다.
앞은 착해 보이는 답이고 뒤는 경험이 있어야 고른다. 관심이 아니라 다른 것을 쟀다.

**판정.** 상위 두 계열까지만 "상대적으로 더 관심을 보인 활동"으로 표시한다.
한 계열을 적성으로 확정하지 않고, 직업이나 학과를 지정하지 않는다.

**아이가 관심 분야를 직접 골랐으면 그쪽을 우선한다.** 흥미 문항은 대조 정보다.
어긋나면 "틀렸다"가 아니라 "두 방향의 활동을 모두 경험해 보는 편이 좋겠습니다"로 쓴다.

## 8. 응답 성의 확인

**모순 쌍을 쓰지 않는다.** 전에 쓰던 세 쌍은 서로 다른 상황이라 같은 답을 기대할 근거가 없었다.

- 응답 속도가 문항당 평균 3초 미만
- `freq5` 네 문항과 `interest4` 여섯 문항이 **둘 다** 한 값으로 직진

**한쪽만으로는 불성실로 보지 않는다.** A를 여러 번 골랐다는 이유만으로 표시하지 않는다.
문구는 의심하는 말투를 쓰지 않는다. "답변이 빠르게 진행된 문항이 몇 개 있습니다."

## 9. 물어놓고 안 쓰는 문항 0개

모든 채점·진단 문항에 `decisionUse`를 단다. RULES나 결과 설명 어느 쪽에서도 참조되지 않으면 빌드 테스트가 실패한다.

## 9-1. 진입 비밀번호

**들어올 때마다 묻는다.** 통과 여부를 `localStorage`에 저장하지 않고 메모리(`passed`)에만 둔다. 저장하면 한 번 통과한 브라우저는 영영 안 묻게 되고, 구독이 끊긴 뒤에도 계속 열린다.

통과하면 **하던 데서 이어진다.** 게이트는 `phase`를 건드리지 않는다. 중간에 닫았다 다시 열면 비밀번호만 한 번 치고 이어서 답한다.

'처음부터 다시'도 게이트를 통과시키지 않는다. 답만 지우고 화면을 다시 그린다.

비밀번호 평문은 소스에 두지 않고 SHA-256만 둔다(`GATE_HASH`). `tests/run.mjs`의 I23이 평문·저장·되돌리기 구멍을 전부 막는다.

## 10. 저장

기존 Supabase 테이블(`public.responses`, `payload jsonb`)을 그대로 쓴다.
새 응답에는 `schemaVersion: "v3.1"`을 넣는다. 과거 v2 응답은 관리자 화면에서 버전을 표시하고 구버전 형식으로 열람만 한다.

## 11. 확인이 필요한 항목 (코드와 분리)

- [ ] 과학고·영재학교 전형 일정 — 학년별 안내 문구를 쓰기 전에 해당 연도 요강 원문 확인
- [ ] 지역인재 요건 — 결과에 문장으로 쓰려면 시행령 원문 재확인
- [ ] 집단 퍼센트 4개 — 해당 학년도 모집 정원으로 재계산
- [ ] 학교 목록 98개 — 학년도가 바뀌면 지정 취소·신설을 다시 확인한다 (2027 기준 · 부천·분당중앙과고가 2027 신설)
- [ ] 학군지 시·군·구 20개 — 공식 근거가 없는 편집 목록이다. 학원 수 공시가 갱신되면 다시 본다
- [ ] 지역인재 문장 — 붙여 두었으나 시행령 원문 재확인이 남았다 (현재 근거는 로컬 대조와 지방대육성법 요지)
- [x] 평준화 데이터 — 시·도 17곳 전부 2027학년도 교육청 원문 직독으로 채웠다 (2026-08-26). `근거_평준화_2027.md` 참조
- [ ] 평준화 지망 개수 — 포항·전북·울산은 2026년 9월 공고 예정인 전형요강에서 확정된다. 9월에 다시 읽는다
- [ ] 청주 오송읍 학군 편입 — 2026년 9월 여론조사, 적용은 2028학년도부터라 지금 중3에게는 해당 없다. 학년도가 넘어가면 반영한다

**임의로 최신 법·일정을 만들어 내지 않는다.**
