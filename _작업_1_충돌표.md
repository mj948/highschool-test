# 1-1. 파일 충돌표 (개정 착수 시점)

| 항목 | QUESTIONS.json | RULES.json | index.html | SPEC.md / README / 시작가이드 | 기준으로 삼을 것 |
|---|---|---|---|---|---|
| 버전 | v3.0 | v3.0 | **v2.1** | **v2.1** | **SPEC v3.1 (신규)** |
| 문항 수 | 54 (조건부 4) | — | **45** | **45 (39+G3+H3)** | 새로 산정 |
| 축 이름 | L·T·P·Q·R·S·E·D·B | 같음 | **A·B·C·D·E·F·G·H** | **같음(A~H)** | 새 축 (아래 §3) |
| 판정 방식 | — | **순차 필터 5단계** | **0~100 적합도 점수 + cap** | **적합도 점수** | **순차 필터** |
| 학교 유형 | — | **4개 + 별도분기 2** | **7개(자공고2.0 별도)** | **7개** | **4개 + 분기 2** |
| 학력 판정 | 1-1/1-2a/1-2b | 위치→내부컷 | **G1 6단계 자기보고 + 학력 cap** | 같음 | **v3 방식** |
| 심리 판정 | P·Q·R | **기질 8칸(유형명)** | **Big Five + 멘탈 cap + 유형 6종** | 같음 | **유형명 폐기, 관찰문** |
| 흥미 판정 | F1~F12 양자택일 | 승수 3 이상 | **없음** | 없음 | **6문항 4단계로 교체** |
| 결과 화면 | — | 7화면 | **적합도 막대 + 육각형 + 순위** | 같음 | **의사결정표 3묶음** |
| 자유서술 | 11-1·11-2 | — | **H1·H2·H3** | 같음 | **2개** |
| Supabase 스키마 | — | — | `guide_v2_responses` / payload jsonb | 같음 | **유지 + schemaVersion** |
| 관리자 화면 | — | — | **A~F 점수·Big5·cap 표시** | 같음 | **새 구조로 교체** |
| 구버전 코드 잔존 | — | — | `big5`36 `mentalCap`15 `academicCap`17 `careerCap`12 `typeScores`17 `AXIS_TYPE_WEIGHTS`4 `자공고2.0`27 `적합도`4 | — | **전부 제거** |

## 결론

`index.html`·`SPEC.md`·`README.md`·`CLAUDE_CODE_시작.md`는 **v2.1에서 멈춰 있고 v3와 아무 관계가 없다.**
`QUESTIONS.json`·`RULES.json`만 v3다. 단일 진실 공급원이 없는 상태다.

## 1-2. 새 원칙

```
SPEC.md (v3.1)   사람이 읽는 최상위 설계
QUESTIONS.json   화면에 노출되는 문항
RULES.json       판정 로직
index.html       위 셋의 구현
```

네 파일이 일치해야 한다. **"충돌 시 index.html이 정답"은 폐기한다.**
`WEIGHTS.json`은 운영에서 제거하고 `_archive_v2/`로 옮긴다. 과거 응답 열람용 backwards compatibility만 남긴다.
