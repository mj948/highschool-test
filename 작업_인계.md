# 작업 인계 — 이 저장소를 처음 여는 사람에게

## 먼저 읽을 것

1. `SPEC.md` — 설계 전부
2. `QUESTIONS.json` — 문항
3. `RULES.json` — 판정
4. `index.html` — 구현

## 이 저장소에서 하면 안 되는 것

- 학교 유형에 0~100 적합도 점수를 매기는 것
- 아이별 예상 내신 등급이나 상위 퍼센트를 화면에 내는 것
- 심리 유형 이름을 붙이는 것 (`연료형` `자책형` 같은 조어)
- 성향 하나로 학교 유형을 탈락시키는 것
- 초등생에게 고교 유형 순위를 주는 것
- 흥미 여섯 문항으로 직업·학과를 찍는 것
- 근거 없는 적중률·신뢰도 수치를 만드는 것
- 판정에 쓰지 않는 문항을 남기는 것

## 문항을 고칠 때

문항마다 두 질문에 답할 수 있어야 한다.

1. 이 질문 하나가 정확히 무엇을 재는가
2. 그 정보 때문에 어떤 학교 판정이 달라지는가

둘째에 답할 수 없으면 삭제 후보다. `QUESTIONS.json`의 `decisionUse` 필드가 그 답이고,
`tests/run.mjs`가 참조되지 않는 문항을 찾아 실패시킨다.

## 고치는 순서

```
SPEC.md → QUESTIONS.json → RULES.json → index.html → tests
```

반대로 가지 않는다. `index.html`만 고치고 스펙을 안 고치면 다음 사람이 또 헤맨다.

## v2.1을 찾는다면

`_archive_v2/`에 있다. **기준이 아니다.** 과거 응답 데이터를 열어 볼 때만 참조한다.
